# iv

> 📂 所属分类:01 · 核心因果推断方法 (Core Causal Methods)

statspai.iv — the unified Instrumental Variables namespace.
> 📝 *statspai.iv — 统一的工具变量（Instrumental Variables，IV）命名空间。*

The goal of this subpackage is to be the single entry point for every
> 📝 *此子包的目标是作为 StatsPAI 中所有 IV 相关工作流的单一入口，*
IV-flavoured workflow in StatsPAI, regardless of which sub-module the
> 📝 *无论底层实现在哪个子模块中。*
underlying implementation lives in.

The subpackage itself is **callable**::
> 📝 *    sp.iv("y ~ (d ~ z) + x", data=df)                   # 2SLS（默认）*

    sp.iv("y ~ (d ~ z) + x", data=df)                   # 2SLS (default)
> 📝 *    sp.iv(method="kernel", y=..., endog=..., instruments=..., data=df)*
    sp.iv("y ~ (d ~ z) + x", data=df, method="liml")    # LIML
> 📝 *    sp.iv.fit(...)         # 等价写法（fit = _dispatch 别名）*
    sp.iv(method="kernel", y=..., endog=..., instruments=..., data=df)
> 📝 *    sp.iv.kernel_iv(...)   # 各估计器仍可直接调用*
    sp.iv.fit(...)         # equivalent (fit = _dispatch alias)
    sp.iv.kernel_iv(...)   # individual estimators still reachable
> 📝 *该可调用对象由一个小型 ``ModuleType`` 子类提供，通过 ``sys.modules[__name__].__class__``*

That callable is provided by a tiny ``ModuleType`` subclass installed via
``sys.modules[__name__].__class__`` (the standard PEP 562-style trick).
> 📝 *子方法覆盖范围（``method=`` 关键字，所有别名均自动转为小写）：*

Sub-method coverage (``method=`` keyword, all aliases lowercased):
> 📝 ***K-class 公式路径**（``regression.iv``）：*

- **K-class formula path** (``regression.iv``):
> 📝 ***现代 JIVE 变体**（``iv.jive_variants``）：*
  ``2sls`` / ``tsls`` / ``iv``,  ``liml``,  ``fuller``,  ``gmm``,  ``jive``.
> 📝 *``jive1``，``ujive``，``ijive``，``rjive``。*
- **Modern JIVE variants** (``iv.jive_variants``):
> 📝 ***多弱工具变量**（``iv.many_weak``）：*
  ``jive1``, ``ujive``, ``ijive``, ``rjive``.
> 📝 *``jive_mw``，``many_weak_ar``。*
- **Many-weak** (``iv.many_weak``):
> 📝 ***Lasso / post-Lasso**：*
  ``jive_mw``, ``many_weak_ar``.
> 📝 *``lasso``（``regression.advanced_iv.lasso_iv``），*
- **Lasso / post-Lasso**:
> 📝 *``rlasso`` / ``rigorous_lasso``（``rlasso.rlasso_iv`` — 忠实移植自 ``hdm::rlassoIV``；*
  ``lasso`` (``regression.advanced_iv.lasso_iv``),
> 📝 *默认进行工具变量筛选，当给定 ``exog=`` 控制变量时使用双重筛选），*
  ``rlasso`` / ``rigorous_lasso`` (``rlasso.rlasso_iv`` — faithful
> 📝 *``post_lasso`` / ``bch``（``iv.post_lasso.bch_post_lasso_iv``，*
  ``hdm::rlassoIV`` port; instrument selection by default, double
> 📝 ***已弃用** — 由 ``rlasso`` 取代）。*
  selection when ``exog=`` controls are given),
> 📝 ***机器学习 / 非参数方法**：*
  ``post_lasso`` / ``bch`` (``iv.post_lasso.bch_post_lasso_iv``,
> 📝 *``kernel``（``iv.kernel_iv``），``npiv``（``iv.npiv``），*
  **deprecated** — superseded by ``rlasso``).
> 📝 *``ivdml``（``iv.ivdml``），``deepiv``（``deepiv.deepiv``，可选）。*
- **ML / nonparametric**:
> 📝 ***贝叶斯方法**（``iv.bayesian_iv``）：``bayes`` / ``bayesian``。*
  ``kernel`` (``iv.kernel_iv``), ``npiv`` (``iv.npiv``),
> 📝 ***局部平均处理效应（LATE）/ 边际处理效应（MTE）**：*
  ``ivdml`` (``iv.ivdml``), ``deepiv`` (``deepiv.deepiv``, optional).
> 📝 *``continuous_late``（``iv.continuous_late``），*
- **Bayesian** (``iv.bayesian_iv``):  ``bayes`` / ``bayesian``.
> 📝 *``mte``（``iv.mte``），*
- **LATE / MTE**:
> 📝 *``ivmte_bounds``（``iv.ivmte_lp``）。*
  ``continuous_late`` (``iv.continuous_late``),
> 📝 ***分位数 IV**（``regression.iv_quantile``）：``ivqreg`` / ``quantile``。*
  ``mte`` (``iv.mte``),
> 📝 ***近似外生敏感性分析**（``iv.plausibly_exogenous``）：*
  ``ivmte_bounds`` (``iv.ivmte_lp``).
> 📝 *``plausibly_exog_uci`` / ``plausibly_exog_ltz``。*
- **Quantile IV** (``regression.iv_quantile``):  ``ivqreg`` / ``quantile``.
> 📝 ***偏移份额工具变量 / Shift-Share**（``bartik``）：``shift_share`` / ``bartik``。*
- **Plausibly exogenous sensitivity** (``iv.plausibly_exogenous``):
  ``plausibly_exog_uci`` / ``plausibly_exog_ltz``.
> 📝 *诊断函数（``anderson_rubin_test``、``effective_f_test``、*
- **Shift-share** (``bartik``):  ``shift_share`` / ``bartik``.
> 📝 *``kleibergen_paap_rk``、``sanderson_windmeijer``、``conditional_lr_test``）*

Diagnostics (``anderson_rubin_test``, ``effective_f_test``,
``kleibergen_paap_rk``, ``sanderson_windmeijer``, ``conditional_lr_test``)
remain standalone — they are not estimators and intentionally do not show
up in the ``method=`` table.

Examples
> 📝 *示例*
--------
> 📝 *--------*
>>> import numpy as np
>>> import pandas as pd
>>> import statspai as sp
>>> rng = np.random.default_rng(0)
>>> n = 200
>>> z1, z2, x1, u = (rng.normal(size=n), rng.normal(size=n),
...                  rng.normal(size=n), rng.normal(size=n))
>>> d = 0.7 * z1 + 0.5 * z2 + 0.3 * x1 + u + rng.normal(size=n)
>>> y = 1.0 + 1.5 * d + 0.4 * x1 + u + rng.normal(size=n)
>>> df = pd.DataFrame({"y": y, "d": d, "z1": z1, "z2": z2, "x1": x1})
>>> # Standard 2SLS with a rich diagnostic panel
>>> res = sp.iv("y ~ (d ~ z1 + z2) + x1", data=df)
>>> bool(res.params["d"] > 0)
> 📝 *>>> # 标准 2SLS，含丰富的诊断输出面板*
True

>>> # Sensitivity to exclusion-restriction violations
> 📝 *>>> # 对排他性约束（exclusion restriction）违背的敏感性分析*
>>> chr = sp.iv(
...     method="plausibly_exog_ltz",
...     y="y", endog="d", instruments=["z1", "z2"],
...     gamma_mean=0.0, gamma_var=0.01, data=df,
... )

>>> # Marginal treatment effects (binary endogenous treatment)
> 📝 *>>> # 边际处理效应（Marginal Treatment Effects）（二值内生处理变量）*
>>> prop = 1 / (1 + np.exp(-(0.8 * z1 + 0.3 * x1 + u)))
>>> dbin = (rng.uniform(size=n) < prop).astype(float)
>>> ybin = 1.0 + 1.5 * dbin + 0.4 * x1 + u + rng.normal(size=n)
>>> df_mte = pd.DataFrame({"y": ybin, "d": dbin, "z": z1, "x": x1})
>>> m = sp.iv(method="mte",
...          y="y", endog="d", instruments=["z"], exog=["x"], data=df_mte)

**20 个公共函数**

### `sp.BayesianIVResult()`

**Bayesian IV result with optional per-instrument LATE posteriors.**
> 📝 ***贝叶斯 IV 结果，含可选的逐工具变量 LATE（局部平均处理效应，Local Average Treatment Effect）后验。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `ess` | `number` | ✓ |  | ess parameter (float). |
| `estimand` | `string` | ✓ |  | estimand parameter (str). |
| `hdi_lower` | `number` | ✓ |  | hdi_lower parameter (float). |
| `hdi_prob` | `number` |  | `0.95` | hdi_prob parameter (float). |
| `hdi_upper` | `number` | ✓ |  | hdi_upper parameter (float). |
| `instrument_labels` | `array[string]` |  | `None` | instrument_labels parameter (List[str]). |
| `instrument_summaries` | `object` |  | `None` | instrument_summaries parameter (Dict[str, Dict[str, float]]). |
| `method` | `string` | ✓ |  | Estimator or algorithm variant to use. |
| `model_info` | `object` |  | `None` | model_info parameter (Dict[str, Any]). |
| `n_obs` | `integer` | ✓ |  | Number of obs. |
| `posterior_mean` | `number` | ✓ |  | posterior_mean parameter (float). |
| `posterior_median` | `number` | ✓ |  | posterior_median parameter (float). |
| `posterior_sd` | `number` | ✓ |  | posterior_sd parameter (float). |
| `prob_positive` | `number` | ✓ |  | prob_positive parameter (float). |
| `prob_rope` | `number` |  |  | prob_rope parameter (Optional[float]). |
| `rhat` | `number` | ✓ |  | rhat parameter (float). |
| `trace` | `string` |  |  | trace parameter. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.BayesianIVResult(method="value", estimand="value", posterior_mean=1.0, posterior_median=1.0, posterior_sd=1.0, hdi_lower=1.0, hdi_upper=1.0, prob_positive=1.0, rhat=1.0, ess=1.0, n_obs=1.0)
print(result.summary())
```

---
### `sp.ContinuousLATEResult()`

**Continuous-instrument LATE on the maximal complier class.**
> 📝 ***最大依从者类别（maximal complier class）上的连续工具变量 LATE。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `ci` | `array[string]` | ✓ |  | ci parameter (tuple). |
> 📝 *ci 参数（元组）。*
| `complier_share` | `number` | ✓ |  | complier_share parameter (float). |
| `estimate` | `number` | ✓ |  | estimate parameter (float). |
> 📝 *estimate 参数（浮点数）。*
| `n_obs` | `integer` | ✓ |  | Number of obs. |
| `se` | `number` | ✓ |  | se parameter (float). |
> 📝 *se 参数（浮点数）。*

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.ContinuousLATEResult(estimate=1.0, se=1.0, ci=["a", "b"], complier_share=1.0, n_obs=1.0)
print(result.summary())
```

---
### `sp.IVDiagResult()`

**Container for :func:`iv_diag` output.**
> 📝 ***:func:`iv_diag` 输出的容器。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` | ✓ |  | Significance level for confidence intervals and tests. |
> 📝 *置信区间和检验的显著性水平。*
| `ar_ci` | `array[string]` | ✓ |  | ar_ci parameter (Tuple[float, float]). |
| `ar_pvalue` | `number` | ✓ |  | ar_pvalue parameter (float). |
| `ar_stat` | `number` | ✓ |  | ar_stat parameter (float). |
| `beta_2sls` | `number` | ✓ |  | beta_2sls parameter (float). |
| `beta_ols` | `number` | ✓ |  | beta_ols parameter (float). |
| `bootstrap_ci_pairs` | `array[string]` |  |  | bootstrap_ci_pairs parameter (Optional[Tuple[float, float]]). |
| `bootstrap_ci_wild` | `array[string]` |  |  | bootstrap_ci_wild parameter (Optional[Tuple[float, float]]). |
| `bootstrap_n` | `integer` |  | `0` | bootstrap_n parameter (int). |
| `bootstrap_se_pairs` | `number` |  |  | bootstrap_se_pairs parameter (Optional[float]). |
| `bootstrap_se_wild` | `number` |  |  | bootstrap_se_wild parameter (Optional[float]). |
| `ci_analytic_2sls` | `array[string]` | ✓ |  | ci_analytic_2sls parameter (Tuple[float, float]). |
| `ci_ols` | `array[string]` | ✓ |  | ci_ols parameter (Tuple[float, float]). |
| `clr_ci` | `array[string]` |  |  | clr_ci parameter (Optional[Tuple[float, float]]). |
| `diagnostics` | `object` |  | `None` | diagnostics parameter (Dict[str, Any]). |
| `effective_F` | `number` | ✓ |  | effective_F parameter (float). |
| `endog` | `string` | ✓ |  | endog parameter (str). |
| `exog` | `array[string]` | ✓ |  | exog parameter (List[str]). |
| `first_stage_F` | `number` | ✓ |  | first_stage_F parameter (float). |
| `instruments` | `array[string]` | ✓ |  | instruments parameter (List[str]). |
| `k_ci` | `array[string]` |  |  | k_ci parameter (Optional[Tuple[float, float]]). |
| `kp_rk_f` | `number` | ✓ |  | kp_rk_f parameter (Optional[float]). |
| `kp_rk_lm` | `number` | ✓ |  | kp_rk_lm parameter (Optional[float]). |
| `kp_rk_lm_pvalue` | `number` | ✓ |  | kp_rk_lm_pvalue parameter (Optional[float]). |
| `ltz_ci` | `array[string]` |  |  | ltz_ci parameter (Optional[Tuple[float, float]]). |
| `ltz_warning` | `string` |  |  | ltz_warning parameter (Optional[str]). |
| `n` | `integer` | ✓ |  | n parameter (int). |
| `n_endog` | `integer` | ✓ |  | Number of endog. |
| `n_exog` | `integer` | ✓ |  | Number of exog. |
| `n_instruments` | `integer` | ✓ |  | Number of instruments. |
| `p_2sls` | `number` | ✓ |  | p_2sls parameter (float). |
| `p_ols` | `number` | ✓ |  | p_ols parameter (float). |
| `raw` | `object` |  | `None` | raw parameter (Dict[str, Any]). |
| `se_2sls` | `number` | ✓ |  | se_2sls parameter (float). |
| `se_ols` | `number` | ✓ |  | se_ols parameter (float). |
| `tF_adjusted_ci` | `array[string]` | ✓ |  | tF_adjusted_ci parameter (Tuple[float, float]). |
| `tF_critical_value` | `number` | ✓ |  | tF_critical_value parameter (float). |
| `t_2sls` | `number` | ✓ |  | t_2sls parameter (float). |
| `t_ols` | `number` | ✓ |  | t_ols parameter (float). |
| `tsls_late_caveat` | `string` |  |  | tsls_late_caveat parameter (Optional[str]). |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.IVDiagResult(n=1.0, n_endog=1.0, n_instruments=1.0, n_exog=1.0, endog="value", instruments=["a", "b"], exog=["a", "b"], alpha=1.0, beta_2sls=1.0, se_2sls=1.0, t_2sls=1.0, p_2sls=1.0, ci_analytic_2sls=["a", "b"], beta_ols=1.0, se_ols=1.0, t_ols=1.0, p_ols=1.0, ci_ols=["a", "b"], first_stage_F=1.0, effective_F=1.0, tF_critical_value=1.0, tF_adjusted_ci=["a", "b"], ar_stat=1.0, ar_pvalue=1.0, ar_ci=["a", "b"], kp_rk_lm=1.0, kp_rk_lm_pvalue=1.0, kp_rk_f=1.0)
print(result.summary())
```

---
### `sp.IVRegression()`

**Instrumental Variables regression model.**
> 📝 ***工具变量（IV）回归模型。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `X_endog` | `string` |  |  | Alternative to formula interface. |
| `X_exog` | `string` |  |  | Alternative to formula interface. |
| `Z` | `string` |  |  | Alternative to formula interface. |
| `data` | `string` |  |  | pandas DataFrame containing the variables used by the estimator. |
| `formula` | `string` |  |  | Formula with IV syntax: ``"y ~ (endog ~ z1 + z2) + exog1 + exog2"`` |
| `fuller_alpha` | `number` |  | `1.0` | Fuller constant (only used when method='fuller'). ``alpha=1`` gives the bias-corrected Fuller estimator; ``alpha=4`` minimises MSE under normal errors. |
| `method` | `string` |  | `'2sls'` | Estimation method. |
| `var_names` | `object` |  |  | Alternative to formula interface. |
| `y` | `string` |  |  | Alternative to formula interface. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.IVRegression(formula="y ~ x1 + x2", data=df)
print(result.summary())
```

---
### `sp.KernelIVResult()`

**Output of kernel IV regression.**
> 📝 ***核 IV 回归的输出。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `bandwidth` | `number` | ✓ |  | Bandwidth used for local smoothing or kernel weighting. |
| `ci_high` | `string` | ✓ |  | ci_high parameter (np.ndarray). |
| `ci_low` | `string` | ✓ |  | ci_low parameter (np.ndarray). |
| `grid` | `string` | ✓ |  | grid parameter (np.ndarray). |
| `h_hat` | `string` | ✓ |  | h_hat parameter (np.ndarray). |
| `n_obs` | `integer` | ✓ |  | Number of obs. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.KernelIVResult(grid="value", h_hat="value", ci_low="value", ci_high="value", bandwidth=1.0, n_obs=1.0)
print(result.summary())
```

---
### `sp.W()`

**Spatial weights matrix with CSR-sparse backing.**
> 📝 ***以 CSR（压缩稀疏行，Compressed Sparse Row）稀疏格式为后端的空间权重矩阵。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `id_order` | `array[string]` |  |  | Explicit ordering of observation ids. Defaults to ``sorted(neighbors)``. |
| `neighbors` | `array[string]` | ✓ |  | Mapping observation id -> list of neighbour ids. |
| `weights` | `array[string]` |  |  | Matching weight values. If ``None``, binary (1.0) weights are used. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.W(neighbors=["a", "b"])
print(result.summary())
```

> 📁 See also: `docs/guides/gpu_acceleration.md`, `docs/guides/proximal_family.md`

---
### `sp.anderson_rubin_ci()`

**Anderson-Rubin confidence set -- re-export of**
> 📝 ***Anderson-Rubin 置信集 — 重新导出自***

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.anderson_rubin_ci()
print(result.summary())
```

---
### `sp.anderson_rubin_test()`

**Anderson-Rubin (1949) test -- size-correct under weak instruments. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).**
> 📝 ***Anderson-Rubin（1949）检验 — 在弱工具变量下保持正确检验水平（size）。验证状态：已验证证据层（已知真实值、参考实现、外部一致性或蒙特卡洛模拟验证）。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level. |
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `endog` | `string` | ✓ |  | Endogenous regressor (single). |
| `exog` | `array[string]` |  |  | Included exogenous controls. |
| `h0` | `number` |  | `0` | Null hypothesis value for the endogenous coefficient. |
| `instruments` | `array[string]` | ✓ |  | Excluded instruments. |
| `vcov` | `string (enum)` |  | `'HC1'` | Variance estimator used for the Olea-Pflueger effective F reported alongside AR. |
| `y` | `string` | ✓ |  | Outcome variable. |

> **vcov** options: `'HC0'`, `'HC1'`, `'classic'`

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.anderson_rubin_test(y="outcome", data=df, endog="value", instruments=["a", "b"])
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`, `docs/guides/migration-from-r.md`, `docs/guides/sensitivity_analysis.md`

---
### `sp.conditional_lr_ci()`

**Moreira (2003) CLR confidence set -- re-export of**

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.conditional_lr_ci()
print(result.summary())
```

---
### `sp.continuous_iv_late()`

**LATE with a continuous instrument (Xie et al. 2025). Estimates the LATE on the maximal complier class via quantile-bin Wald ratios, weighted by the bin-pair with the largest first-stage response. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).**
> 📝 ***使用连续工具变量的 LATE（Xie et al. 2025）。通过分位数区间 Wald 比率估计最大依从者类别上的 LATE，以具有最大第一阶段响应的区间对进行加权。验证状态：已验证证据层（已知真实值、参考实现、外部一致性或蒙特卡洛模拟验证）。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level for confidence intervals and tests. |
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `instrument` | `string` | ✓ |  | Continuous instrument |
| `n_boot` | `integer` |  | `200` | Number of bootstrap replications. |
| `n_quantiles` | `integer` |  | `4` | Number of instrument quantile bins |
| `seed` | `integer` |  | `0` | Random seed for reproducible stochastic steps. |
| `treat` | `string` | ✓ |  | Treatment indicator or first-treatment-period column. |
> 📝 *处理指示变量或首次处理周期列。*
| `y` | `string` | ✓ |  | Outcome variable column name or outcome array. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.continuous_iv_late(y="outcome", data=df, treat="value", instrument="value")
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`, `docs/guides/v1_2_frontier.md`

---
### `sp.effective_f_test()`

**Olea-Pflueger (2013) robust effective F statistic for weak instruments. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).**
> 📝 ***Olea-Pflueger（2013）针对弱工具变量的稳健有效 F 统计量。验证状态：已验证证据层（已知真实值、参考实现、外部一致性或蒙特卡洛模拟验证）。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `endog` | `string` | ✓ |  | Endogenous regressor (single endogenous variable). |
| `exog` | `array[string]` |  |  | Included exogenous controls (a constant is added automatically). |
| `instruments` | `array[string]` | ✓ |  | Excluded instruments. |
| `vcov` | `string (enum)` |  | `'HC1'` | Variance estimator for the first-stage residuals: - ``'classic'`` -- homoskedastic; F_eff equals first-stage F. - ``'HC0'`` -- White heteroskedasticity-robust. - ``'HC1'`` -- HC0 with small-sample correction ``n/(n-k)``. |

> **vcov** options: `'HC0'`, `'HC1'`, `'classic'`

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.effective_f_test(data=df, endog="value", instruments=["a", "b"])
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`, `docs/guides/migration-from-r.md`

---
### `sp.iv()`

**Unified IV estimation: 2SLS, LIML, Fuller, GMM, JIVE. Includes first-stage F, Sargan/Hansen J, and Hausman diagnostics. Validation: certified parity evidence.**
> 📝 ***统一 IV 估计：2SLS、LIML、Fuller、GMM、JIVE。包含第一阶段 F 统计量、Sargan/Hansen J 检验和 Hausman 诊断。验证状态：已认证一致性证据。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `cluster` | `string` |  |  | Column name for cluster-robust SEs |
| `data` | `string` | ✓ |  | pandas DataFrame |
| `formula` | `string` | ✓ |  | IV formula: 'y ~ (endog ~ instruments) + exog' |
| `fuller_alpha` | `number` |  | `1.0` | Fuller constant (method='fuller' only) |
| `method` | `string (enum)` |  | `'2sls'` | Estimation method |
| `robust` | `string (enum)` |  | `'nonrobust'` | Standard error type |

> **method** options: `'2sls'`, `'liml'`, `'fuller'`, `'gmm'`, `'jive'`

> **robust** options: `'nonrobust'`, `'hc0'`, `'hc1'`, `'hc2'`, `'hc3'`

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.iv(formula="lwage ~ x1 + x2", data=df)
print(result.summary())
```

> 📁 See also: `docs/guides/diagnostics.md`, `docs/guides/exporting-regression-tables.md`

---
### `sp.iv_compare()`

**Run several k-class / JIVE estimators on the same IV specification and return a one-row-per-method comparison DataFrame (estimate, SE, CI, first-stage F). Useful as a sensitivity sanity check before reporting.**
> 📝 ***在相同 IV 设定上运行多个 k-class / JIVE 估计器，并返回每种方法一行的比较 DataFrame（估计值、SE、CI、第一阶段 F 统计量）。适合作为报告前的敏感性稳健性检查。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level for confidence intervals and tests. |
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `endog_name` | `string` |  |  | Override endogenous-coefficient name lookup |
| `formula` | `string` | ✓ |  | Model formula using patsy/R-style syntax. |
| `methods` | `array[string]` |  | `['2sls', 'liml', 'fuller', 'jive']` | methods parameter (tuple[str]). |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.iv_compare(formula="lwage ~ x1 + x2", data=df)
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`

---
### `sp.iv_diag()`

**Modern IV reporting bundle (R `ivDiag` analogue). Combines 2SLS point estimate, analytic + pairs/wild bootstrap SEs, Olea-Pflueger effective F, Lee-McCrary-Moreira-Porter (2022) tF-corrected critical value, Anderson-Rubin / CLR / K weak-IV-robust confidence sets, Kleibergen-Paap rk LM, Conley-Hansen-Rossi (2012) plausibly-exogenous LTZ sensitivity, and a Blandhol-Bonney-Mogstad-Torgovitsky (2022/2025) / Soczynski (2024) `TSLS-as-LATE` caveat into a single IVDiagResult. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).**
> 📝 ***现代 IV 报告包（R `ivDiag` 的对应物）。将 2SLS 点估计、解析 + 成对/野生自举标准误、Olea-Pflueger 有效 F、Lee-McCrary-Moreira-Porter（2022）tF 校正临界值、Anderson-Rubin/CLR/K 弱 IV 稳健置信集、Kleibergen-Paap rk LM、Conley-Hansen-Rossi（2012）近似外生 LTZ 敏感性以及 Blandhol-Bonney-Mogstad-Torgovitsky（2022/2025）/ Soczynski（2024）`TSLS-as-LATE` 注意事项整合到单个 IVDiagResult 中。验证状态：已验证证据层（已知真实值、参考实现、外部一致性或蒙特卡洛模拟验证）。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level for confidence intervals and tests. |
| `boot_methods` | `array[string]` |  | `['pairs']` | Subset of {'pairs','wild'} |
| `cluster` | `string` |  |  | Cluster column for cluster-robust SE / cluster bootstrap |
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `endog` | `string` | ✓ |  | Single endogenous regressor |
| `exog` | `array[string]` |  |  | Optional included exogenous controls |
| `h0` | `number` |  | `0.0` | Null hypothesis for AR/CLR/K |
| `include_clr_ci` | `boolean` |  | `False` | Whether to include clr ci. |
| `include_k_ci` | `boolean` |  | `False` | Whether to include k ci. |
| `instruments` | `array[string]` | ✓ |  | instruments parameter (list[str] \| str). |
| `ltz_gamma_sd` | `number` |  |  | Standard deviation of CHR (2012) LTZ Gaussian prior on gamma |
| `n_boot` | `integer` |  | `1000` | Bootstrap replications (0 to skip) |
| `random_state` | `integer` |  |  | Random seed or RandomState for reproducible stochastic steps. |
| `vcov` | `string (enum)` |  | `'HC1'` | Heteroskedasticity-robust covariance type |
| `y` | `string` | ✓ |  | Outcome column |

> **vcov** options: `'HC0'`, `'HC1'`, `'classic'`

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.iv_diag(y="outcome", data=df, endog="value", instruments=["a", "b"])
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`

---
### `sp.ivreg()`

**Two-stage least squares (2SLS) IV regression. Alias for sp.iv(method='2sls'). Validation: certified parity evidence.**
> 📝 ***两阶段最小二乘法（2SLS）IV 回归。sp.iv(method='2sls') 的别名。验证状态：已认证一致性证据。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `data` | `string` | ✓ |  | pandas DataFrame |
| `formula` | `string` | ✓ |  | IV formula: 'y ~ (endog ~ instruments) + exog' |
| `robust` | `string` |  | `'nonrobust'` | Standard error type |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.ivreg(formula="lwage ~ x1 + x2", data=df)
print(result.summary())
```

> 📁 See also: `card_iv.py`, `docs/guides/choosing_iv_estimator.md`, `docs/guides/diagnostics.md`

---
### `sp.kernel_iv()`

**Kernel IV regression with uniform confidence bands (Lob et al. 2025). Estimates the structural function h*(d) = E[Y | do(D=d)] via kernel-weighted local averaging under a continuous instrument Z, with wild-bootstrap uniform SEs.**
> 📝 ***带一致置信带的核 IV 回归（Lob et al. 2025）。在连续工具变量 Z 下，通过核加权局部平均估计结构函数 h*(d) = E[Y | do(D=d)]，使用野生自举法获得一致标准误。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level for confidence intervals and tests. |
| `bandwidth` | `number` |  |  | Silverman default |
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `grid` | `string` |  |  | Grid of d-values (default 30 quantile-evenly spaced) |
| `instrument` | `string` | ✓ |  | Continuous instrument Z |
| `n_boot` | `integer` |  | `100` | Number of bootstrap replications. |
| `ridge` | `number` |  | `0.001` | Tikhonov regularisation |
| `seed` | `integer` |  | `0` | Random seed for reproducible stochastic steps. |
| `treat` | `string` | ✓ |  | Continuous treatment D |
| `y` | `string` | ✓ |  | Outcome variable column name or outcome array. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.kernel_iv(y="outcome", data=df, treat="value", instrument="value")
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`, `docs/guides/v1_2_frontier.md`

---
### `sp.lasso_iv()`

**LASSO-selected instrumental variables.**
> 📝 ***LASSO（Least Absolute Shrinkage and Selection Operator）选取的工具变量。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level for confidence intervals and tests. |
| `cluster` | `string` |  |  | Cluster identifier column for clustered standard errors. |
| `data` | `string` | ✓ |  | pandas DataFrame containing the variables used by the estimator. |
| `penalty` | `string` |  | `'bic'` | Instrument selection criterion: 'bic', 'aic', 'cv'. |
| `robust` | `string` |  | `'robust'` | Robust standard-error or covariance estimator option. |
| `x_endog` | `array[string]` | ✓ |  | x_endog parameter (List[str]). |
| `x_exog` | `array[string]` |  |  | x_exog parameter (Optional[List[str]]). |
| `y` | `string` | ✓ |  | Outcome variable column name or outcome array. |
| `z` | `array[string]` |  |  | Full set of candidate instruments. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.lasso_iv(y="outcome", data=df, x_endog=["a", "b"])
print(result.summary())
```

> 📁 See also: `docs/guides/migration-from-r.md`

---
### `sp.liml()`

**Limited Information Maximum Likelihood (LIML) estimator. Validation: certified parity evidence.**
> 📝 ***有限信息最大似然（LIML，Limited Information Maximum Likelihood）估计器。验证状态：已认证一致性证据。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level for confidence intervals and tests. |
| `cluster` | `string` |  |  | Cluster identifier column for clustered standard errors. |
| `data` | `string` |  |  | pandas DataFrame containing the variables used by the estimator. |
| `formula` | `string` |  |  | Model formula using patsy/R-style syntax. |
| `fuller` | `number` |  |  | Fuller's constant (typically 1 or 4). If None, pure LIML. |
| `robust` | `string` |  | `'nonrobust'` | Robust standard-error or covariance estimator option. |
| `x_endog` | `array[string]` |  |  | Endogenous regressors. |
| `x_exog` | `array[string]` |  |  | Exogenous regressors (included instruments). |
| `y` | `string` |  |  | Outcome variable. |
| `z` | `array[string]` |  |  | Excluded instruments. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.liml(formula="y ~ x1 + x2", data=df)
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`, `docs/guides/migration-from-r.md`

---
### `sp.rlasso_iv()`

**Instrumental-variables estimation with rigorous-Lasso selection.**
> 📝 ***使用严格 Lasso 选取的工具变量估计。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `control` | `object` |  |  | Forwarded to :func:`statspai.rlasso.rlasso` (penalty level, loadings, iteration controls). |
| `d` | `string` | ✓ |  | name). |
| `data` | `string` |  |  | pandas DataFrame containing the variables used by the estimator. |
| `intercept` | `boolean` |  | `True` | Passed to the underlying ``rlasso`` first stages. |
| `penalty` | `object` |  |  | Forwarded to :func:`statspai.rlasso.rlasso` (penalty level, loadings, iteration controls). |
| `post` | `boolean` |  | `True` | Post-Lasso (OLS refit) inside every selection step. |
| `select_X` | `boolean` |  | `True` | Lasso-select among the controls (partialling-out). |
| `select_Z` | `boolean` |  | `True` | Lasso-select among the instruments. |
| `x` | `array[string]` |  |  | Primary running variable, regressor, or feature input for this estimator. |
| `y` | `string` | ✓ |  | name). |
| `z` | `array[string]` | ✓ |  | Instrument, proxy, or auxiliary variable used by this estimator. |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.rlasso_iv(y="outcome", x=["treatment", "covariate1", "covariate2"], data=df, d="value", z=["a", "b"])
print(result.summary())
```

> 📁 See also: `docs/guides/rigorous_lasso_hdm.md`, `docs/guides/sp_dml_vs_doubleml.md`

---
### `sp.tF_critical_value()`

**Lee-McCrary-Moreira-Porter (2022, AER) tF adjusted critical value. Validation: validated evidence tier (known-truth, reference, external-parity, or Monte Carlo artifact).**
> 📝 ***Lee-McCrary-Moreira-Porter（2022, AER）tF 调整临界值。验证状态：已验证证据层（已知真实值、参考实现、外部一致性或蒙特卡洛模拟验证）。***

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `alpha` | `number` |  | `0.05` | Significance level. Only ``0.05`` is implemented (the only level for which LMMP publish a complete table). |
| `first_stage_F` | `number` | ✓ |  | Observed first-stage F statistic (or Olea-Pflueger F_eff). |

**示例：**
> 📝 *示例代码*

```python
import statspai as sp
import pandas as pd

# Load or prepare data
df = sp.datasets.card_1995()  # example dataset

result = sp.tF_critical_value(first_stage_F=1.0)
print(result.summary())
```

> 📁 See also: `docs/guides/choosing_iv_estimator.md`, `docs/guides/migration-from-r.md`

---
